
define(function(require) {
  var abstractManager;
  abstractManager = (function() {

    abstractManager.name = 'abstractManager';

    function abstractManager() {}

    return abstractManager;

  })();
  return abstractManager;
});
